import { ElementBindingDirective } from './element-binding.directive';

describe('ElementBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementBindingDirective();
    expect(directive).toBeTruthy();
  });
});
