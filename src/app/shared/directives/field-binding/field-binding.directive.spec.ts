import { FieldBindingDirective } from './field-binding.directive';

describe('FieldBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldBindingDirective();
    expect(directive).toBeTruthy();
  });
});
